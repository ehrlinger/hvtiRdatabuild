% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_set.R
\name{write_analysis_set}
\alias{write_analysis_set}
\title{Write an analysis set}
\usage{
write_analysis_set(name, cfg = hvtiRutilities::study_config())
}
\arguments{
\item{name}{Character(1). The set's name in \verb{_study.yml}.}

\item{cfg}{List. A study manifest from \code{\link[hvtiRutilities:study_config]{hvtiRutilities::study_config()}}.}
}
\value{
The sidecar contents, invisibly: a list with \code{set}, \code{parent},
\code{declaration_sha256}, \code{written}, \code{counts}, \code{n_cols}, \code{attrition} and
\code{packages}.
}
\description{
Cuts the analysis set \code{name}, declared under \verb{analysis_sets:} in the study's
\verb{_study.yml}, from the built dataset: keeps its \code{vars}, applies its \code{exclude}
rules in order (first match wins), checks any \code{expect} counts, and writes
\verb{datasets/<name>.parquet}, a \verb{datasets/<name>.set.yml} sidecar recording the
parent dataset and the attrition, and a \code{manifest.yaml} entry.
}
\details{
Nothing is written unless every check passes. Each \code{when} is R code evaluated
against the data with only base R visible; a missing value counts as not
excluded, as SAS \verb{if <missing> then delete} does. The identifier column named
by \code{id} is used to track exclusions and is never written to the sidecar or
the manifest.
}
