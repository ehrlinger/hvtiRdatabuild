% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/master_snapshot.R
\name{snapshot_master}
\alias{snapshot_master}
\title{Snapshot a master dataset to parquet, with its lineage}
\usage{
snapshot_master(
  config,
  out_dir,
  which = c("current", "history"),
  chunk_rows = 1e+05,
  expect = NULL
)
}
\arguments{
\item{config}{A \code{master_config} from \code{\link[=read_master_config]{read_master_config()}}.}

\item{out_dir}{Directory to write the parquet snapshots and their sidecars.}

\item{which}{\code{"current"}, \code{"history"}, or both.}

\item{chunk_rows}{Rows per chunk, passed to \code{\link[=snapshot_oracle]{snapshot_oracle()}}.}

\item{expect}{Optional validation for the current build, passed to
\code{\link[=snapshot_oracle]{snapshot_oracle()}}. Historical builds are not validated, because their
logs are rarely retained.}
}
\value{
Invisibly, a data frame with one row per dataset: \code{file}, \code{status}
(\code{"written"}, \code{"skipped"} or \code{"failed"}), \code{n_rows}, \code{n_cols}, \code{sha256},
\code{source_sha256}, \code{parent_release}, \code{parent_source} (\code{"log"}, \code{"program"},
\code{"declared"}, \code{"unknown"} or \code{"none"}) and \code{key_verdict}.
}
\description{
Freezes a master's current SAS build, or its historical builds, as parquet
with \code{\link[=snapshot_oracle]{snapshot_oracle()}}, then checks its keys and records which release of
its parent master it was built from.
}
\details{
The parent release is read from evidence of what ran, in this order: the log
of the run that produced the dataset (a \code{.log} whose timestamp falls within
six hours after the dataset's), where SAS records every dataset it read;
then, for the current build only, the build program's \code{set} statements; then
\code{parent_release} in the configuration. The log comes first because a
program can be edited after the run. A current build whose parent cannot be
decided stops; a historical one with no detected parent records \code{"unknown"}.
\code{parent_release} in the configuration, and the disagreement check against a
detected parent, apply only to the current build: a declared release
describes the build as configured today, not necessarily what an older
historical dataset actually read.

Only \verb{NOTE:} lines naming datasets are read from a log, never data lines.
Nothing printed carries a key or a value.
}
\examples{
\donttest{
if (requireNamespace("arrow", quietly = TRUE) &&
    requireNamespace("jsonlite", quietly = TRUE) &&
    requireNamespace("tidyselect", quietly = TRUE)) {
  dir <- tempfile("master")
  dir.create(dir)
  file.copy(system.file("extdata", "oracle_small.sas7bdat",
                        package = "hvtiRdatabuild"),
            file.path(dir, "built.sas7bdat"))
  writeLines("data m; run;", file.path(dir, "bd.data.sas"))
  cfg_path <- file.path(dir, "master.yml")
  writeLines(c("name: master_demo", "key: [ccfidu]",
               paste0("snapshots: ", dir), "current: built.sas7bdat",
               paste0("build_program: ", file.path(dir, "bd.data.sas"))),
             cfg_path)
  snapshot_master(read_master_config(cfg_path), tempfile("out"),
                  which = "current")
}
}

}
\seealso{
\code{\link[=read_master_config]{read_master_config()}}, \code{\link[=lift_master]{lift_master()}}
}
