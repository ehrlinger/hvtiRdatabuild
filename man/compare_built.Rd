% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_built.R
\name{compare_built}
\alias{compare_built}
\title{Compare an R-built dataset against a SAS oracle}
\usage{
compare_built(oracle, r, id = "ccfidu", tolerance = 1e-08)
}
\arguments{
\item{oracle}{Data frame read from the parquet oracle. See
\code{\link[=snapshot_oracle]{snapshot_oracle()}}.}

\item{r}{Data frame produced by the R pipeline.}

\item{id}{Name of the identifier column present in both. Must be unique
within each.}

\item{tolerance}{Numeric. \strong{Absolute} numeric differences at or below
this are reported as \code{"within_tolerance"}. A single absolute threshold
cannot serve columns of very different magnitude; see the "Coming from
SAS" vignette for how to read \code{max_rel_diff} alongside it.}
}
\value{
An object of class \code{built_comparison}: a data frame with one row per
variable and columns \code{variable}, \code{n_common}, \code{verdict}, \code{n_differ},
\code{max_abs_diff}, \code{max_rel_diff}, and \code{detail}. \code{n_common} is the number of
rows compared (constant across all rows, so it survives \code{write.csv()} or
\code{as.data.frame()}, unlike the \code{rows} attribute). \code{verdict} is one of
\code{"identical"}, \code{"within_tolerance"}, \code{"differs"}, \code{"absent_in_r"},
\code{"absent_in_sas"}, or \code{"type_mismatch"}. The \code{rows} attribute holds a
list with \code{n_oracle}, \code{n_r}, \code{n_common}, \code{only_oracle}, and \code{only_r}.
}
\description{
Joins \code{r} to \code{oracle} on \code{id} and classifies every variable. Row-set
differences are reported separately from value differences: an identifier
present on one side only is a cohort discrepancy, a different class of
problem from a miscomputed variable, and conflating the two hides both.
}
\details{
This function deliberately returns a table and never a single pass/fail.
Collapsing several hundred variables into one boolean launders real
differences into a green check. Read the table.

A difference does not automatically mean the R code is wrong. SAS orders
missing numerics below all values, so \verb{if bmi < 18.5} classifies a missing
BMI as underweight where R yields \code{NA}. Record such cases as
\code{intentional_divergence} in \code{equivalence_signoff.yaml}.
}
\examples{
o <- data.frame(ccfidu = c("A1", "A2"), age = c(65, 70))
r <- data.frame(ccfidu = c("A1", "A2"), age = c(65, 71))
compare_built(o, r, id = "ccfidu")

}
\seealso{
\code{\link[=snapshot_oracle]{snapshot_oracle()}}
}
