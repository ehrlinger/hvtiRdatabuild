% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dw_pull.R
\name{dw_pull}
\alias{dw_pull}
\title{Pull the enabled warehouse modules for a study}
\usage{
dw_pull(config, conn)
}
\arguments{
\item{config}{A \code{study_config} from \code{\link[=read_study_config]{read_study_config()}}.}

\item{conn}{A \link[DBI:DBIConnection-class]{DBI::DBIConnection}, typically from \code{\link[=dw_connect]{dw_connect()}}.}
}
\value{
An object of class \code{pull_result}: a list with \code{tables}, a named
list of data frames keyed by each module's \code{output} name, and \code{manifest},
a data frame with columns \code{module}, \code{output}, \code{n_rows}, \code{n_cols}, and
\code{pulled_at}.
}
\description{
Executes one query per module named in the study configuration and returns
the raw tables alongside a manifest of what was pulled. This is the R port
of \code{tp.stXXXX_dwpull.sas}.
}
\details{
The port is \strong{read-only}. The SAS templates also upload a cohort table to
the warehouse; that write is deliberately not carried here and is deferred
to a later slice with its own review.

A module that returns zero rows is an error unless its definition declares
it optional. A silently empty module produces a quietly incomplete dataset,
which is the failure this package exists to prevent.
}
\examples{
\donttest{
# Requires a warehouse connection; see vignette("building-a-study-dataset")
# for a runnable version against a mocked connection.
}

}
\seealso{
\code{\link[=dw_connect]{dw_connect()}}, \code{\link[=dw_modules]{dw_modules()}}, \code{\link[=compare_built]{compare_built()}}
}
