% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_set.R
\name{read_analysis_set}
\alias{read_analysis_set}
\title{Read an analysis set}
\usage{
read_analysis_set(name, cfg = hvtiRutilities::study_config())
}
\arguments{
\item{name}{Character(1). The set's name in \verb{_study.yml}.}

\item{cfg}{List. A study manifest from \code{\link[hvtiRutilities:study_config]{hvtiRutilities::study_config()}}.}
}
\value{
A data frame of the set's columns and rows, with the per-rule
attrition table attached as \code{attr(x, "attrition")}.
}
\description{
Reads the analysis set \code{name} written by \code{\link[=write_analysis_set]{write_analysis_set()}}, after
checking that it is current. It stops, naming the \code{write_analysis_set()} call
that fixes it, when the built dataset has changed since the set was written,
when the set's declaration in \verb{_study.yml} has changed, or when the parquet no
longer matches its manifest entry. A stale set is never rebuilt silently:
its exclusions are decisions, and a changed attrition should be looked at.
}
\seealso{
\code{\link[=write_analysis_set]{write_analysis_set()}}
}
