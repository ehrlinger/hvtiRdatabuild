% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_publication.R
\name{withdraw_dataset_release}
\alias{withdraw_dataset_release}
\title{Withdraw a published dataset release}
\usage{
withdraw_dataset_release(
  dataset_id,
  release_id,
  datasets_dir,
  reason,
  replacement_release_id = NULL
)
}
\arguments{
\item{dataset_id}{Character. Stable logical dataset identifier.}

\item{release_id}{Character. Exact release identifier to withdraw.}

\item{datasets_dir}{Character. Existing directory that owns the published
files and \code{dataset-catalog.yml}.}

\item{reason}{Character. Non-empty reason for withdrawal.}

\item{replacement_release_id}{Optional character release identifier from the
same logical dataset.}
}
\value{
Invisibly, the release record with \code{status = "withdrawn"},
\code{withdrawal_reason}, and \code{replacement_release_id} when supplied.
}
\description{
Withdrawal changes the release's catalog status and records why it should no
longer be adopted. It never changes or deletes the release file. A study
already pinned to that release can therefore reproduce historical work with
the explicit \code{allow_withdrawn = TRUE} override in
\code{\link[hvtiRutilities:read_built]{hvtiRutilities::read_built()}}, while normal reads stop and report the
withdrawal.
}
\details{
A replacement is optional, but it must name another release under the same
logical dataset. Catalog locking serializes withdrawal with publication, so
neither operation can discard the other's catalog change.
}
\examples{
published <- tempfile("published-")
dir.create(published)
draft <- tempfile(fileext = ".csv")
write.csv(data.frame(synthetic_id = "SYN001", value = 1), draft,
          row.names = FALSE)
release <- publish_dataset(
  draft,
  "example_cohort",
  published,
  extract_date = "2026-09-21"
)
withdrawn <- withdraw_dataset_release(
  "example_cohort",
  release$release_id,
  published,
  reason = "Synthetic example correction"
)
withdrawn$status
file.exists(file.path(published, release$file))
unlink(c(draft, published), recursive = TRUE)

}
