% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/master_corrections.R
\name{decide_correction}
\alias{decide_correction}
\title{Decide a correction: accept, reject, supersede or bake it}
\usage{
decide_correction(
  config,
  con,
  correction_id,
  decision,
  decided_by,
  reason = NA_character_,
  dry_run = FALSE,
  dialect = "mssql"
)
}
\arguments{
\item{config}{A \code{master_config} from \code{\link[=read_master_config]{read_master_config()}}.}

\item{con}{A DBI connection.}

\item{correction_id}{Character. The correction being decided.}

\item{decision}{Character. One of \code{DECISIONS}.}

\item{decided_by}{Character. Who is deciding.}

\item{reason}{Character, or \code{NA_character_}, the default. A note on the
decision.}

\item{dry_run}{If \code{TRUE}, validate and return the row without writing it.
The default, \code{FALSE}, writes it.}

\item{dialect}{\code{"mssql"} or \code{"duckdb"}.}
}
\value{
Invisibly, a list with \code{verdict} (\code{"recorded"}, or \code{"validated"}
on a dry run), \code{decision_id}, and \code{row} (the one-row data frame that was
or would be appended).
}
\description{
Records the decision in the master's decisions table; the correction
applies only when its latest decision is \code{"accept"}. \code{dry_run = TRUE}
validates and returns the row that would be written, without writing it.
}
\details{
Requires the master's corrections tables, which
\code{\link[=backfill_corrections]{backfill_corrections()}} creates.
}
\seealso{
\code{\link[=backfill_corrections]{backfill_corrections()}}, \code{\link[=propose_correction]{propose_correction()}}
}
