% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_publication.R
\name{publish_dataset}
\alias{publish_dataset}
\title{Publish an immutable dataset release}
\usage{
publish_dataset(
  draft,
  dataset_id,
  datasets_dir,
  extract_date = Sys.Date(),
  source = NULL,
  file_stem = dataset_id
)
}
\arguments{
\item{draft}{Character. Path to a mutable draft dataset.}

\item{dataset_id}{Character. Stable logical dataset identifier, using
lower-case letters, digits, and underscores.}

\item{datasets_dir}{Character. Existing directory that owns the published
files and \code{dataset-catalog.yml}.}

\item{extract_date}{A \code{Date} or \code{YYYY-MM-DD} string. Defaults to today.}

\item{source}{Optional character string recording publisher provenance.}

\item{file_stem}{Character. Safe basename stem for dated release files.
Defaults to \code{dataset_id}.}
}
\value{
Invisibly, a named list containing \code{release_id}, \code{sequence}, \code{file},
\code{extract_date}, \code{revision}, \code{published_at}, \code{sha256}, \code{n_rows}, \code{n_cols},
\code{status}, and \code{source} when supplied.
}
\description{
A draft is the file a programmer is still allowed to rebuild. Publication
copies those bytes to a dated release file and adds the release to
\code{dataset-catalog.yml}. Consuming studies can then discover the release, but
they keep reading the release they already pinned until someone explicitly
adopts the new one with \code{\link[hvtiRutilities:adopt_data_update]{hvtiRutilities::adopt_data_update()}}.
}
\details{
\code{publish_dataset()} reads the draft, stages a byte-for-byte copy in
\code{datasets_dir}, and reads the staged copy again before recording its SHA-256,
row count, and column count. It supports the clinical-data formats read by
\code{\link[hvtiRutilities:read_clinical_data]{hvtiRutilities::read_clinical_data()}}: \code{.sas7bdat}, \code{.csv}, \code{.xlsx},
\code{.xls}, and \code{.rds}. This verifies that the release is readable and records
its shape. It does not decide whether the cohort or its values are clinically
correct.

The first release on a date is named \verb{<file_stem>_YYYYMMDD.<ext>}. A
different release published for the same extract date adds \verb{_r2}, \verb{_r3},
and so on. The catalog lock protects that sequence when two processes publish
at once. Publishing the same bytes again for the same dataset and date is
idempotent: it returns the existing release rather than creating a revision.
If that release was withdrawn, it remains withdrawn; publication does not
reactivate the bytes under a new release identity.

The release file moves into place before the catalog is replaced. If the
catalog write fails, the file remains as an unregistered orphan. No study can
discover it. Retry the same call and publication registers the matching
orphan without rewriting its bytes. Different bytes at an expected release
filename are an integrity error and are never overwritten.
}
\examples{
published <- tempfile("published-")
dir.create(published)
draft <- tempfile(fileext = ".csv")
write.csv(
  data.frame(synthetic_id = c("SYN001", "SYN002"), value = c(1, 2)),
  draft,
  row.names = FALSE
)
release <- publish_dataset(
  draft,
  dataset_id = "example_cohort",
  datasets_dir = published,
  extract_date = "2026-09-21",
  source = "Synthetic documentation example"
)
release$release_id
unlink(c(draft, published), recursive = TRUE)

}
